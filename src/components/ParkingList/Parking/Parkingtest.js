/* eslint-disable */

/* import { render, screen } from '@testing-library/react';
// import { faker } from '@faker-js/faker';

import Parking from './Parking';

test('should renders a parking name', () => {
  // Arrange
  const parking = {
    name: 'PARQUEADERO VER',
    addres: 'cra. 6 #13104',
    cityName: 'CALI',
    totalPlaces: 80,
    hourValue: 3200,
    position: {
      latitude: 3.4517820866804323,
      longitude: -76.53005763194443,
    }
  }

  render(<Parking parkings={parking} />);

  // Act
  const divElement = screen.getByRole('contentinfo');

  // Assert
  expect(divElement).toBeInTheDocument();
  expect(divElement).toHaveTextContent(`${parking.name}`);
  // expect(divElement).toHaveAttribute('role', 'contentinfo');
});
*/
